/**
 * Adversary reaction — Overwhelm (SRD)
 *
 * TODO REACTION [RESOURCE]: Adversary Stress mark for movement/ability (per SRD).
 * TODO REACTION [AURA]: Range-limited effect on PCs (per SRD).
 * TODO REACTION [TRIGGER]: Reaction window — detect event, optional costs, then resolve (per SRD).
 * TODO REACTION [ATTACK]: Roll / apply standard attack from statblock (`adversary-roll-descriptors.js`).
 */
export const OverwhelmReaction = {
  name: "Overwhelm",
  type: 'reaction',
  description: "When the Zombies mark HP from an attack within Melee range, you can **mark a Stress** to make a standard attack against the attacker.",
};
